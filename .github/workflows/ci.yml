name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  kernel-quality:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mote-kernel
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.85.0
          components: rustfmt
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: mote-kernel/pyproject.toml
      - uses: astral-sh/setup-uv@v7
        with:
          version: "0.12.3"
          enable-cache: true
          cache-dependency-glob: |
            mote-infra/persistence/cloudflare/python/uv.lock
            mote-resource/container/cloudflare/python/uv.lock
      - uses: pnpm/action-setup@v4
        with:
          version: 10.30.3
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: pnpm
          cache-dependency-path: |
            mote-infra/persistence/cloudflare/ts/pnpm-lock.yaml
            mote-resource/container/cloudflare/ts/pnpm-lock.yaml
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e '.[dev]'
      - run: uv sync --locked
        working-directory: mote-infra/persistence/cloudflare/python
      - run: uv sync --locked
        working-directory: mote-resource/container/cloudflare/python
      - run: pnpm install --frozen-lockfile
        working-directory: mote-infra/persistence/cloudflare/ts
      - run: pnpm install --frozen-lockfile
        working-directory: mote-resource/container/cloudflare/ts
      - run: pre-commit run --all-files
        working-directory: .
      - run: python -m ruff check src tests
      - run: python -m ruff format --check src tests
      - run: make typecheck
      - run: python -m pytest --cov=mote_kernel --cov-report=term-missing --cov-report=xml
      - run: python -m build
      - run: python -m twine check dist/*

  kernel-tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    defaults:
      run:
        working-directory: mote-kernel
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: mote-kernel/pyproject.toml
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e '.[dev]'
      - run: python -m pytest

  persistence-local-quality:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: mote-infra/persistence/local
    env:
      CARGO_TERM_COLOR: always
      RUST_BACKTRACE: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.85.0
          components: clippy, rustfmt
      - run: make check
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-deny@0.19.7
      - run: cargo deny check

  persistence-local-current-rust-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: mote-infra/persistence/local
    env:
      CARGO_TERM_COLOR: always
      RUST_BACKTRACE: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo +stable test --locked --all-targets --all-features

  persistence-cloudflare-python-quality:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: mote-infra/persistence/cloudflare/python
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: "3.13"
      - uses: astral-sh/setup-uv@v7
        with:
          version: "0.12.3"
          enable-cache: true
          cache-dependency-glob: mote-infra/persistence/cloudflare/python/uv.lock
      - run: uv sync --locked
      - run: make check

  persistence-cloudflare-ts-quality:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: mote-infra/persistence/cloudflare/ts
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          package_json_file: mote-infra/persistence/cloudflare/ts/package.json
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: pnpm
          cache-dependency-path: mote-infra/persistence/cloudflare/ts/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: pnpm run check

  container-cloudflare-ts-quality:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: mote-resource/container/cloudflare/ts
    env:
      WRANGLER_SEND_METRICS: "false"
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          package_json_file: mote-resource/container/cloudflare/ts/package.json
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: pnpm
          cache-dependency-path: mote-resource/container/cloudflare/ts/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: pnpm run check:ci

  container-cloudflare-ts-node-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        node-version: ["22.19", "24", "26"]
    defaults:
      run:
        working-directory: mote-resource/container/cloudflare/ts
    env:
      WRANGLER_SEND_METRICS: "false"
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          package_json_file: mote-resource/container/cloudflare/ts/package.json
      - uses: actions/setup-node@v5
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm
          cache-dependency-path: mote-resource/container/cloudflare/ts/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: pnpm run test

  container-cloudflare-python-quality:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: mote-resource/container/cloudflare/python
    env:
      WRANGLER_SEND_METRICS: "false"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: "3.13"
      - uses: astral-sh/setup-uv@v7
        with:
          version: "0.12.3"
          enable-cache: true
          cache-dependency-glob: mote-resource/container/cloudflare/python/uv.lock
      - uses: pnpm/action-setup@v4
        with:
          package_json_file: mote-resource/container/cloudflare/python/package.json
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: pnpm
          cache-dependency-path: mote-resource/container/cloudflare/python/pnpm-lock.yaml
      - run: uv sync --locked
      - run: pnpm install --frozen-lockfile
      - run: make check
