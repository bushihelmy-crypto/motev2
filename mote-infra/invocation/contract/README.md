# Invocation contracts

Reserved for narrow typed contracts owned by the invocation infrastructure. Add a contract only with a concrete caller and implementation; cross-language observable schemas remain in `conformance/`.
