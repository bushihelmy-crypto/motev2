# HTTP transport

Reserved for a concrete Mote HTTP transport adapter. Keep request/response schemas owned by the boundary that defines the protocol; this directory should provide transport mechanics only.
