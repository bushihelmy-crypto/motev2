# Configuration examples

This directory is reserved for local development examples. It is not an
authoritative configuration schema. Service credentials must be represented as
secret references, and a checked-in example must never contain a real token.

When configuration becomes an observable contract, define its versioned schema
under the repository's conformance owner first and generate/validate examples
from that single source.
