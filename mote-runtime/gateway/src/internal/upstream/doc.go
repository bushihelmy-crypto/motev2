// Package upstream owns outbound network contracts and concrete transports used
// by protocol adapters and service connectors. It never parses inbound RPC or
// applies model capability rules.
package upstream
