// Package model owns exact model identity, capabilities, and the immutable
// model catalog. Catalog defaults are patched once by validated
// Kernel invocation configuration. It never owns endpoint, credential,
// service, protocol, routing, or pricing configuration.
package model
