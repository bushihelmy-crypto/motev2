// Package protocol owns wire-protocol adapter contracts, protocol facts, and
// concrete adapters. Protocol adapters translate payloads but never resolve
// credentials or hosts; static composition maps them without a global registry.
package protocol
