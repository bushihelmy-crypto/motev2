# Canonical traces

Canonical observable traces describe stable event ordering and identity relationships. They must not encode language-specific stack frames, coroutine scheduling, logging text, or private object layout.
